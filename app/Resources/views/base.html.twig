<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Symfony中文站{% endblock %}</title>
        <meta name="keywords" content="Symfony,Symfony2,Symfony框架,Symfony framework,Symfony中文,Symfony文档" />
        <meta name="description" content="Symfony中文网,Symfony2中文文档" />
        {% block stylesheets %}
            <style type="text/css">
                body, *{ margin: 0px; padding: 0px; color: #444; font-family: 'Helvetica Neue', Arial, 'Liberation Sans', FreeSans, 'Hiragino Sans GB', sans-serif; font-size: 14px;}
                body{ width: 900px;  margin:auto; background-color: #fff;}
                a{ color: #444; text-decoration: none; }
                a:hover{ text-decoration: underline;}
                h1{ font-size: 35px; margin: 50px 0px; text-align: center;}
                h2{ margin: 35px 0px; font-size: 25px; border-bottom: 1px solid #ccc; padding-bottom: 5px;}
                h3{ margin: 25px 0px; font-size: 20px;}
                h4{ margin: 15px 0px; font-size: 18px;}
                img{ padding: 5px;margin: 10px 0 32px 0; border: 1px solid #ccc; margin: 0 auto; max-width: 880px; }
                ul, ol, dl{ margin: 10px 40px;  }
                ul{ color: #888; font-size: 80%;  padding-left: 5px; }
                ul#articles li{ color: #888; font-size: 80%; border-bottom: 1px solid #eee; list-style: none; padding-left: 5px;}
                ul#articles li a {color: #047; font-size: 15px; padding: 18px; display: inline-block; text-decoration: none;}
                ul#articles li span{ float: right; font-size: 15px; padding: 18px; color:#888;}
                li{  padding:10px; }
                p{ margin-bottom: 25px; line-height: 25px; }
                p em{ background-color: #f5f5f5; padding:15px; width: 100%; display: block; font-style: normal; }
                pre code{ color: #fff; }
                pre code.hljs{ color:#fff; line-height: 20px; padding: 15px; margin-bottom: 25px; overflow: auto; }
            </style>
            {% stylesheets '@FilixTheBookBundle/Resources/public/css/*' %}
                <link rel="stylesheet" href="{{ asset_url }}" />
            {% endstylesheets %}
        {% endblock %}
    </head>
    <body id="body">
        {% block header %}{% endblock header %}
        {% block content %}{% endblock content %}
        {% block javascripts %}
        <script type="text/javascript">
        var _bdhmProtocol = (("https:" == document.location.protocol) ? " https://" : " http://");
        document.write(unescape("%3Cscript src='" + _bdhmProtocol + "hm.baidu.com/h.js%3F366039750dde3608e47647df9185af64' type='text/javascript'%3E%3C/script%3E"));
        </script>
        {% endblock %}
        {% block footer %}{% endblock footer %}
    </body>
</html>
